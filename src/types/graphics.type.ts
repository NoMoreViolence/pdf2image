export type GraphicOptions = {
  quality?: number | string;
  format?: string;
  size?: string | number;
  density?: string | number;
  savedir?: string;
  savename?: string;
  compression?: string;
};

